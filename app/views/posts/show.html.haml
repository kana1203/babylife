.main
  .content
    .content__post{:style => "background-image: url(#{@post.image});"}
      .more
        %span= image_tag 'arrow_top.png'
        %ul.more_list
          %li
            = link_to '編集', "/posts/#{@post.id}/edit", method: :get
          %li
            = link_to '削除', "/posts/#{@post.id}", method: :delete
      .more_title
        = @post.title
        %span.name
          %a{:href => "/users/#{@post.user.id}"}
            %span 投稿者
            = @post.user.name
    .content__text
      = @post.text